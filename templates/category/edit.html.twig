{% extends 'base.html.twig' %}
{% block title %}Edit Category{% endblock %}
{% block body %}
<h1>Edit Category</h1>
{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    <button class="btn btn-primary">Update</button>
{{ form_end(form) }}
<a href="{{ path('category_index') }}" class="btn btn-secondary mt-2">Back to list</a>
{% endblock %}
